
[1]
	#If true, commands will run each time the world is joined instead of just once
	Repeat = false
	#If true, commands will run for each player instead of just for the first join
	DedicatedMode = false
	#If true, commands will only run if the game is fully restarted
	OnFullRestart = false
	#list of all the commands to run, in order
	Commands = ["/gamerule artifacts.runningShoes.doIncreaseStepHeight false", "/gamerule artifacts.runningShoes.speedBonus 15", "/gamerule artifacts.feralClaws.attackSpeedBonus 30", "/gamerule artifacts.panicNecklace.speedDuration 5", "/gamerule artifacts.panicNecklace.speedLevel 2", "/gamerule artifacts.panicNecklace.cooldown 10", "/gamerule artifacts.powerGlove.attackDamageBonus 1", "/gamerule artifacts.obsidianSkull.fireResistanceCooldown 29", "/gamerule artifacts.antidoteVessel.maxEffectDuration 1", "/gamerule artifacts.vampiricGlove.maxHealingPerHit 4", "/gamerule artifacts.vampiricGlove.absorptionRatio 5", "/gamerule artifacts.vampiricGlove.absorptionChance 100", "/gamerule artifacts.thornPendant.strikeChance 100", "/gamerule artifacts.crystalHeart.healthBonus 20"]

[2]
	#this new set of commands uses different settings from the other 2 and will run second
	Repeat = false
	DedicatedMode = false
	OnFullRestart = false
	#command "wsc null" does not do anything
	Commands = ["wsc null"]

[3]
	#this new set of commands uses different settings from the other 2 and will run third, you cannot add new ones after this for now, this is badly programmed I know I'll make it more consistent one day
	Repeat = false
	DedicatedMode = false
	OnFullRestart = false
	#command "wsc null" does not do anything
	Commands = ["wsc null"]

